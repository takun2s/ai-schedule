spring:
  datasource:
    url: jdbc:mysql://mysql:3306/scheduler?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    
  redis:
    host: redis
    port: 6379
    
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

zookeeper:
  connect-string: zookeeper:2181

minio:
  endpoint: http://minio:9000
  access-key: minioadmin
  secret-key: minioadmin

logging:
  level:
    com.scheduler: DEBUG
